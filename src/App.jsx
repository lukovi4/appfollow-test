import ReviewsFeed from './scenes/ReviewsFeed.jsx'

// Корень приложения. Пока одна сцена — позже сюда же добавим переключатель сцен (Reviews feed /
// Add automation / редизайн), оборачивающий несколько сцен.
export default function App() {
  return <ReviewsFeed />
}
